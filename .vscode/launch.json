{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "name": "Debug",
            "request": "launch",
            "program": "${workspaceFolder}/bonus",
            // "env": {
            //     "LSAN_OPTIONS": "verbosity=1:log_threads=1"
            // },
            "args": [
                "maps/bonus.ber"
            ],
            "cwd": "${workspaceFolder}",
        }
    ]
}